# Copy this to a file named .env in the project root and fill the values
# (Don't commit .env; it's already ignored by .gitignore)

REACT_APP_SUPABASE_URL=https://your-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key
